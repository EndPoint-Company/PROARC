<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PROARC.src.Views.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PROARC.src.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel>
        <!-- Banner com imagem -->
        <Image Source="Assets/banner.png" HorizontalAlignment="Stretch" VerticalAlignment="Top" Width="Auto" Height="200"/>

        <!-- Texto ou Botão abaixo da imagem do banner (opcional) -->
        <TextBlock Text="Bem-vindo ao Processo de Registro!" FontSize="24" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <Button Content="Clique para Registrar Processo" Background="Transparent" BorderBrush="White" Foreground="White" Click="BannerButton_Click"/>

        <!-- Frame para o conteúdo abaixo do banner -->
        <Frame x:Name="contentFrame" VerticalAlignment="Top" HorizontalAlignment="Stretch" Margin="0,10,0,0"/>
    </StackPanel>

</Page>
